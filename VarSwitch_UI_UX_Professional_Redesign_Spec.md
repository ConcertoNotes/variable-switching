# VarSwitch UI/UX 专业评审与重构实施文档

> 文档用途：本文件用于对 VarSwitch 当前界面进行专业产品设计评审，并提供可直接交给 Codex / Cursor 执行的 UI 重构需求说明。<br>
> 目标读者：产品负责人、UI/UX 设计师、前端开发、Codex 自动化编码助手。<br>
> 适用范围：VarSwitch 桌面端 Tauri 应用，包括主界面、Claude Code 配置、Codex CLI 配置、Codex Toolbox、插件市场、会话同步、手机控制、添加配置弹窗、导入配置弹窗等。

---

## 0. 总体结论

当前 VarSwitch 已经具备较完整的功能能力，包括：

- Claude Code 环境变量管理；
- Codex CLI 配置管理；
- 当前配置同步；
- Codex 配置健康诊断；
- 插件市场；
- 会话同步；
- 手机控制；
- 配置导入与新增；
- 多语言、主题切换、帮助入口等基础功能。

但是当前界面存在一个核心问题：

> 功能已经形成产品雏形，但 UI 信息架构、视觉层级、交互路径和组件规范还停留在“功能堆叠型工具页”阶段，尚未形成专业桌面软件级别的控制台体验。

因此，本次改版不应只是“换颜色”“改圆角”“加毛玻璃”，而应该做一次系统级 UI/UX 重构。

推荐的新产品定位为：

> VarSwitch = AI 开发环境控制台 / AI Runtime 配置管理器 / Codex & Claude Code 环境中枢。

用户打开 VarSwitch 后，第一眼应该知道：

1. 当前正在使用哪个配置；
2. Claude Code 是否正常；
3. Codex CLI 是否正常；
4. 环境变量是否同步成功；
5. 插件、会话、手机控制是否可用；
6. 如果异常，下一步应该怎么修复；
7. 如果正常，下一步可以执行什么操作。

---

## 1. 当前界面专业评审

### 1.1 信息架构问题

当前页面采用顶部栏 + 大 Hero 区 + 横向 Tab + 下方卡片的布局。这个结构在功能较少时可用，但 VarSwitch 现在已经包含多个复杂模块：

- Claude Code；
- Codex CLI；
- 配置管理；
- 插件市场；
- 会话同步；
- 手机控制；
- 诊断；
- 设置；
- 帮助；
- 导入与添加配置。

这些模块的功能性质不同，但当前被混合放在同一个页面层级中，导致用户难以判断“哪个是主要功能，哪个是辅助入口”。

#### 当前表现

- 首页上方的“使用说明 / 检查更新 / 下载网站 / GitHub 仓库 / Codex Toolbox”全部以卡片形式并列展示；
- Claude Code 与 Codex CLI 作为横向 Tab 出现，但它们各自包含的信息复杂度不同；
- Codex Toolbox 内部又使用横向 Tab 承载插件市场、会话同步、手机控制；
- 添加配置、导入配置、工具入口、语言切换、主题切换、设置图标全部集中在顶部区域。

#### 评审意见

当前架构会造成以下问题：

1. 核心任务不突出；
2. 功能之间的关系不清晰；
3. 顶部栏承载了过多低频功能；
4. 用户需要滚动页面才能看到关键状态；
5. 工具箱中的复杂功能被弱化成 Tab，后续扩展困难；
6. 页面长时间发展后容易变成“所有功能都在首页”的堆叠结构。

#### 修改方向

应改为“左侧主导航 + 顶部状态栏 + 主内容区”的桌面控制台结构。

推荐一级导航：

```text
Overview 概览
Claude Code
Codex CLI
Configurations 配置管理
Plugin Market 插件市场
Session Sync 会话同步
Mobile Control 手机控制
Settings 设置
```

推荐顶部栏只保留：

```text
产品名 / 当前运行状态 / 当前配置 / 添加配置 / 设置入口
```

语言切换、主题切换、帮助、更新、GitHub、下载网站等低频入口应移动到 Settings 或 Help Center。

---

### 1.2 首页定位问题

当前首页上方 Hero 区视觉面积较大，展示：

- VarSwitch Console；
- 产品说明；
- 使用说明；
- 检查更新；
- 下载网站；
- GitHub 仓库；
- Codex Toolbox。

这类设计更接近营销页或欢迎页，但 VarSwitch 是一个配置管理型工具。用户打开软件时最关心的不是介绍文案，而是：

- 当前环境是否可用；
- 当前配置是否生效；
- 当前 Claude / Codex 状态是否正常；
- 是否需要同步或修复；
- 如何快速切换配置。

#### 评审意见

Hero 区占用过多首屏空间，会导致真正关键的配置状态、健康诊断、配置列表下移。尤其在 1000px 左右高度的窗口中，用户需要滚动才能完整查看状态信息。

#### 修改方向

首页应改为 Dashboard，而不是 Welcome Page。

首页目标：

> 用最短路径展示当前环境状态，并给出下一步操作。

推荐首页结构：

```text
Overview Dashboard

1. 当前环境总览
   - 当前主配置
   - Claude Code 状态
   - Codex CLI 状态
   - 系统环境变量状态
   - 最近同步时间

2. 健康状态矩阵
   - Claude Code
   - Codex CLI
   - Cursor 配置
   - 系统环境变量
   - 插件状态
   - 会话同步状态
   - 手机控制状态

3. 快捷操作
   - 添加配置
   - 导入当前配置
   - 立即同步
   - 打开插件市场
   - 打开 Codex Toolbox
   - 查看诊断详情

4. 最近配置
   - 当前使用配置
   - 可切换配置
   - 最近编辑配置

5. 最近事件 / 操作日志
   - 最近同步成功
   - 读取 Cursor 配置失败
   - 插件修复完成
   - 微信绑定成功
```

---

### 1.3 顶部栏问题

当前顶部栏包含：

- Logo；
- 产品名；
- 副标题；
- 中文 / EN；
- 白天 / 夜间；
- 多个图标入口；
- 导入当前配置；
- 添加配置。

#### 评审意见

顶部栏承载了过多信息，且按钮层级不清晰。

例如：

- “添加配置”是核心主操作；
- “导入当前配置”是次级高频操作；
- 语言与主题是低频设置；
- 图标入口无文字说明，用户无法一眼理解；
- 多个图标之间没有分组，视觉上显得零散。

#### 修改方向

顶部栏应做减法。

推荐顶部栏布局：

```text
┌────────────────────────────────────────────────────────────┐
│ VarSwitch    当前配置：elucid · Codex 正常 · Claude 正常       │
│                                      [导入] [添加配置] [设置] │
└────────────────────────────────────────────────────────────┘
```

或者：

```text
左侧：Logo + VarSwitch
中间：全局状态胶囊
右侧：导入配置 / 添加配置 / 设置
```

顶部栏保留内容：

- Logo；
- 产品名；
- 当前配置状态；
- 当前环境状态；
- 添加配置；
- 设置入口。

移入设置或帮助中心的内容：

- 语言切换；
- 主题切换；
- 检查更新；
- GitHub；
- 下载网站；
- 使用说明；
- 关于；
- 工具图标入口。

---

### 1.4 横向 Tab 问题

当前主界面使用 Claude Code / Codex CLI 两个横向 Tab。Codex Toolbox 内部也使用插件市场 / 会话同步 / 手机控制横向 Tab。

#### 评审意见

横向 Tab 适合少量、同级、内容结构类似的页面。但当前这些模块并非同类型内容：

- Claude Code 是环境变量配置；
- Codex CLI 是 CLI 配置和健康诊断；
- 插件市场是扩展安装；
- 会话同步是数据管理；
- 手机控制是远程控制。

它们功能差异较大，不适合长期都放在横向 Tab 中。

#### 修改方向

主模块改为左侧一级导航。

Codex Toolbox 内部改为左侧二级导航或独立一级页面。

推荐结构：

```text
左侧主导航
- Overview
- Claude Code
- Codex CLI
- Configurations
- Plugin Market
- Session Sync
- Mobile Control
- Settings
```

如果保留 Toolbox 概念，则 Toolbox 页面内部使用：

```text
Codex Toolbox
├── Plugin Market
├── Session Sync
└── Mobile Control
```

但从产品清晰度来看，更建议把插件市场、会话同步、手机控制直接提升为一级导航。

---

### 1.5 视觉层级问题

当前界面使用了大量卡片、渐变、毛玻璃、圆角、阴影。整体方向不错，但存在一个问题：

> 几乎所有区域都在强调，导致真正重要的信息反而不突出。

例如：

- Hero 区很大；
- 状态卡片也很大；
- 健康检测卡片使用渐变；
- 配置列表卡片也带明显边框；
- 多个按钮颜色较强；
- 成功、失败、使用中、删除等标签都在抢视觉注意力。

#### 评审意见

视觉设计应该建立明确层级：

1. 当前状态最重要；
2. 当前配置次重要；
3. 快捷操作第三；
4. 详细诊断信息第四；
5. 帮助、说明、路径等为最低优先级。

#### 修改方向

减少大面积渐变和毛玻璃，把视觉重点留给状态和操作。

推荐视觉原则：

- 背景保持干净；
- 主卡片使用白色或接近白色；
- 渐变只用于首页状态总览或空状态；
- 成功 / 警告 / 错误色只用于状态标签，不大面积铺色；
- 所有卡片统一圆角、边框、间距；
- 主按钮只保留一个高亮色；
- 危险按钮只在危险操作出现时使用红色。

---

### 1.6 原生弹窗问题

截图中出现了浏览器原生 prompt：

```text
tauri.localhost 显示
请输入导入的 Codex 配置名称
```

#### 评审意见

这是当前界面中最影响专业感的问题之一。

原生 prompt / alert / confirm 会带来：

- 视觉风格与应用不一致；
- 用户体验突兀；
- 不能承载复杂说明；
- 不能进行输入校验展示；
- 不利于国际化和主题统一；
- 让软件显得像开发测试阶段产物。

#### 修改方向

禁止在产品代码中使用：

```javascript
alert()
confirm()
prompt()
```

全部替换为自定义 Dialog / Modal / Toast。

例如“导入当前配置”应改成：

```text
导入当前 Codex 配置

配置名称
[ 当前 Codex 配置             ]

说明：
系统将从本地 Codex 配置文件读取 API Key、Base URL、模型等信息，
并保存为一个新的 VarSwitch 配置项。

[取消] [导入配置]
```

---

### 1.7 添加配置弹窗问题

当前“添加 Codex 配置”弹窗内容较长，且内部滚动。表单项包括：

- 预设；
- 配置名称；
- 写入方式；
- API Key；
- Base URL；
- 模型；
- 测试；
- 其他信息。

#### 评审意见

这个弹窗是核心流程，但当前呈现方式会让用户产生压力。尤其是写入方式、官方账号登录、默认写入、auth.json、config.toml 这些技术概念都出现在同一页，用户容易不知道怎么选。

#### 修改方向

添加配置改成 Stepper 分步向导。

推荐流程：

```text
步骤 1：选择配置类型 / 预设
步骤 2：选择写入方式
步骤 3：填写连接信息
步骤 4：测试连接
步骤 5：保存并启用
```

详细说明：

#### Step 1：选择预设

字段：

- 自定义；
- OpenAI；
- GetElucid；
- OpusRelay；
- 其他内置供应商。

说明：

- 预设只负责自动填充 Base URL、Provider、默认模型；
- 用户仍可手动修改。

#### Step 2：选择写入方式

选项一：默认写入

```text
写入 ~/.codex/auth.json 和 ~/.codex/config.toml
推荐大多数用户使用。
```

选项二：仅写入配置文件

```text
只写入 ~/.codex/config.toml，不写入认证文件。
适合使用官方账号登录的场景。
```

选项三：仅保存到 VarSwitch

```text
只保存配置，不立即写入本机环境。
适合提前准备多个配置。
```

#### Step 3：填写连接信息

字段：

- 配置名称；
- API Key；
- Base URL；
- 模型；
- Provider；
- Auth 类型。

字段行为：

- API Key 默认密码显示；
- Base URL 支持一键测试；
- 模型可手动输入，也可从预设选择；
- 配置名称必填；
- Base URL 必填；
- API Key 根据写入方式决定是否必填。

#### Step 4：测试连接

展示检测项：

- API Key 格式；
- Base URL 可访问性；
- 模型可用性；
- 本地文件写入权限；
- Codex 配置目录存在性。

检测结果用状态列表展示：

```text
API Key 格式        正常
Base URL 连接       正常
模型可用性          正常
配置文件权限        正常
```

#### Step 5：保存

操作按钮：

- 保存；
- 保存并启用；
- 返回修改。

---

## 2. 新版产品信息架构

### 2.1 一级导航

新版 VarSwitch 建议采用如下一级导航：

```text
VarSwitch
├── Overview 概览
├── Claude Code
├── Codex CLI
├── Configurations 配置管理
├── Plugin Market 插件市场
├── Session Sync 会话同步
├── Mobile Control 手机控制
└── Settings 设置
```

### 2.2 页面职责定义

#### Overview 概览

用于展示全局状态，不承载复杂编辑任务。

包含：

- 当前配置；
- Claude Code 状态；
- Codex CLI 状态；
- 系统环境变量状态；
- 插件状态；
- 会话同步状态；
- 手机控制状态；
- 快捷操作；
- 最近事件。

#### Claude Code

用于管理 Claude Code 环境变量与相关编辑器配置。

包含：

- 当前 Claude 配置；
- 系统环境变量；
- Cursor 配置；
- Claude 设置；
- 同步按钮；
- 诊断详情。

#### Codex CLI

用于管理 Codex CLI 配置、健康检查和当前生效状态。

包含：

- 当前 Codex 配置；
- Codex 状态；
- 健康诊断；
- 配置列表；
- 操作区。

#### Configurations 配置管理

用于统一管理所有配置。

包含：

- Claude 配置；
- Codex 配置；
- 配置导入；
- 配置导出；
- 配置备份；
- 批量删除；
- 配置复制。

#### Plugin Market 插件市场

用于安装、启用、修复 Codex 插件。

包含：

- 插件源选择；
- 插件搜索；
- 插件分类；
- 插件列表；
- 插件详情；
- 安装 / 启用 / 修复操作。

#### Session Sync 会话同步

用于管理 Codex 会话同步。

包含：

- 同步状态；
- 已同步会话数量；
- 搜索；
- 会话列表；
- 复制 ID；
- 回收站；
- 备份恢复。

#### Mobile Control 手机控制

用于绑定移动端平台，并将手机消息转发到 Codex 会话。

包含：

- 平台绑定；
- 选择控制会话；
- 监听状态；
- 连接状态；
- 消息记录；
- 异常提示。

#### Settings 设置

用于承载低频设置和辅助入口。

包含：

- 语言；
- 主题；
- 数据目录；
- Codex 路径；
- Claude 路径；
- 检查更新；
- GitHub；
- 下载网站；
- 使用说明；
- 关于软件。

---

## 3. 新版布局规范

### 3.1 应用整体布局

推荐使用桌面应用控制台布局：

```text
┌──────────────────────────────────────────────────────────────┐
│ Top Bar                                                       │
├───────────────┬──────────────────────────────────────────────┤
│ Sidebar       │ Main Content                                  │
│               │                                               │
│ Navigation    │ Page Header                                   │
│               │ Page Body                                     │
│               │                                               │
└───────────────┴──────────────────────────────────────────────┘
```

### 3.2 尺寸建议

```text
窗口最小宽度：960px
推荐宽度：1100px - 1440px
左侧导航宽度：220px
顶部栏高度：56px
页面横向 Padding：24px
页面纵向 Padding：24px
卡片间距：16px
卡片内边距：20px / 24px
按钮高度：36px / 40px
输入框高度：40px
圆角：12px / 16px
```

### 3.3 顶部栏规范

顶部栏职责：展示当前应用全局状态与主要操作。

布局建议：

```text
[Logo VarSwitch]     [当前配置: elucid] [Codex 正常] [Claude 正常]      [导入] [添加配置] [设置]
```

注意：

- 不要在顶部栏放过多 icon-only 按钮；
- 所有 icon-only 按钮必须有 Tooltip；
- 低频设置入口收进 Settings；
- 添加配置使用主按钮样式；
- 导入配置使用次级按钮样式。

### 3.4 左侧导航规范

左侧导航包含：

```text
Overview
Claude Code
Codex CLI
Configurations
Plugin Market
Session Sync
Mobile Control
Settings
```

每个导航项包含：

- 图标；
- 文本；
- 可选状态点。

例如：

```text
Overview
Claude Code        ● 正常
Codex CLI          ● 正常
Session Sync       ● 133
Mobile Control     ● 已绑定
```

状态点建议：

- 绿色：正常；
- 黄色：需要注意；
- 红色：异常；
- 灰色：未启用。

### 3.5 页面头部规范

每个页面头部应包含：

- 页面标题；
- 页面说明；
- 主要操作按钮。

例如：

```text
Codex CLI
管理 Codex CLI 的 API Key、Base URL、模型和本地配置文件。

[刷新诊断] [备份文件] [添加 Codex 配置]
```

---

## 4. 视觉设计规范

### 4.1 风格定位

推荐风格：

> Linear + Raycast + Vercel Dashboard + ChatGPT Settings

关键词：

- 专业；
- 干净；
- 轻量；
- 桌面工具感；
- 状态清晰；
- 操作明确；
- 不过度炫技。

### 4.2 色彩规范

推荐颜色：

```text
背景色：#F7F8FA
主卡片背景：#FFFFFF
次级卡片背景：#FAFAFB
边框色：#E5E7EB
主文字：#111827
次级文字：#6B7280
弱文字：#9CA3AF
主色蓝：#2563EB
主色 hover：#1D4ED8
成功绿：#16A34A
成功浅底：#DCFCE7
警告黄：#D97706
警告浅底：#FEF3C7
错误红：#DC2626
错误浅底：#FEE2E2
信息蓝浅底：#DBEAFE
```

### 4.3 渐变使用规范

允许使用渐变的区域：

- Overview 顶部状态总览；
- 健康检测成功区域；
- 空状态页面；
- 品牌装饰背景。

不建议使用渐变的区域：

- 普通配置卡片；
- 表单；
- 表格；
- 大多数弹窗；
- 删除 / 编辑按钮。

原因：

> 过多渐变会削弱信息层级，让专业工具软件显得不够克制。

### 4.4 字体层级

推荐字号：

```text
页面标题：24px / 28px，600-700
区块标题：18px / 22px，600
卡片标题：15px / 18px，600
正文：14px / 20px，400
辅助说明：13px / 18px，400
标签文字：12px / 16px，500
代码 / Key / 路径：13px，monospace
```

### 4.5 圆角与阴影

```text
小组件圆角：8px
按钮圆角：8px / 10px
卡片圆角：14px / 16px
弹窗圆角：18px / 20px
边框：1px solid #E5E7EB
阴影：轻量使用，不要大面积重阴影
```

推荐卡片阴影：

```css
box-shadow: 0 1px 2px rgba(16, 24, 40, 0.04);
```

弹窗阴影：

```css
box-shadow: 0 20px 60px rgba(15, 23, 42, 0.18);
```

---

## 5. 组件设计规范

### 5.1 Button 按钮

按钮类型：

```text
Primary：主要操作，如 添加配置、立即同步、保存并启用
Secondary：次级操作，如 导入、刷新诊断、备份文件
Ghost：轻量操作，如 取消、返回
Danger：危险操作，如 删除、清除绑定、移入回收站
Icon：图标按钮，必须有 Tooltip
```

按钮规则：

- 一个操作区域最多只能有一个 Primary；
- 删除类操作必须使用 Danger；
- 不要用绿色按钮表示“切换使用”，建议使用蓝色或中性按钮；
- 成功状态用标签表达，不用按钮颜色表达；
- Disabled 状态需要明显降低透明度并禁止点击。

### 5.2 Status Badge 状态标签

状态标签用于表达当前状态，不承担操作功能。

推荐状态：

```text
正常 / Healthy / OK
异常 / Error
读取失败 / Failed
使用中 / Active
未启用 / Disabled
已绑定 / Bound
监听中 / Listening
已同步 / Synced
```

示例：

```text
● 正常
● 使用中
● 读取失败
```

颜色规则：

- 正常：绿色；
- 异常：红色；
- 警告：黄色；
- 信息：蓝色；
- 未启用：灰色。

### 5.3 Card 卡片

卡片用途：

- 状态总览；
- 配置项；
- 插件项；
- 会话项；
- 诊断项。

卡片结构：

```text
Card Header
- 标题
- 状态标签 / 操作入口

Card Body
- 主要信息
- 次级信息

Card Footer
- 操作按钮
```

配置卡片示例：

```text
┌────────────────────────────────────────┐
│ elucid                         ● 使用中 │
│                                        │
│ API Key      sk-5fb••••••3b5b   [复制] │
│ Base URL     https://hk.get...  [复制] │
│ Model        gpt-5.5                   │
│                                        │
│ [编辑] [复制配置]              [删除]   │
└────────────────────────────────────────┘
```

### 5.4 Dialog 弹窗

Dialog 必须统一样式，禁止浏览器原生弹窗。

Dialog 结构：

```text
标题
说明文字
内容区域
底部操作区
```

规则：

- 标题清楚说明动作；
- 说明文字解释影响；
- 主按钮在右侧；
- 取消按钮在主按钮左侧；
- 危险操作需要二次确认；
- 表单弹窗不要过长，超过一定复杂度应改为 Stepper。

### 5.5 Toast 通知

Toast 用于轻量反馈：

- 同步成功；
- 复制成功；
- 保存成功；
- 删除成功；
- 检测失败。

错误类 Toast 需要提供下一步操作，例如：

```text
Cursor 配置读取失败
请检查 Cursor 配置文件是否存在。
[查看详情]
```

### 5.6 Empty State 空状态

配置为空时，不要只显示空白。

示例：

```text
还没有 Codex 配置
添加一个 Codex 配置后，你可以一键切换 API Key、Base URL 和模型。

[添加 Codex 配置]
```

---

## 6. 页面级改版方案

## 6.1 Overview 概览页

### 页面目标

让用户一眼判断整个开发环境是否正常。

### 推荐布局

```text
Overview
集中查看 VarSwitch 当前环境状态、配置和快捷操作。

┌──────────────────────────────────────────────┐
│ 当前环境                                     │
│ 当前配置：elucid                             │
│ Codex CLI：正常     Claude Code：正常         │
│ 系统环境变量：已同步                         │
│ 最近同步：刚刚                               │
│ [立即同步] [添加配置] [查看诊断]              │
└──────────────────────────────────────────────┘

┌────────────┬────────────┬────────────┬────────────┐
│ Codex CLI  │ Claude Code │ 插件状态    │ 会话同步    │
│ 正常       │ 正常        │ 5/6 已启用  │ 133 个会话  │
└────────────┴────────────┴────────────┴────────────┘

┌──────────────────────────┬──────────────────────────┐
│ 最近配置                  │ 最近事件                  │
│ elucid 使用中             │ Codex 同步成功             │
│ 自己 可切换               │ Cursor 配置读取失败        │
└──────────────────────────┴──────────────────────────┘
```

### Codex 执行要求

- 将现有 Hero 区改为状态总览卡；
- 将帮助、GitHub、下载、检查更新移至 Settings / Help；
- 概览页只展示关键信息，不展示完整 API Key 和完整路径；
- 添加最近事件列表；
- 所有异常状态必须提供“查看详情”入口。

---

## 6.2 Codex CLI 页面

### 页面目标

管理 Codex CLI 的当前配置、健康状态、配置列表和本地文件同步。

### 推荐布局

```text
Codex CLI
管理 Codex CLI 的 API Key、Base URL、模型和本地配置。

[刷新诊断] [备份 Codex 文件] [添加 Codex 配置]

┌──────────────────────────────────────────────┐
│ Codex CLI 当前状态                            │
│ ● 正常                                        │
│ 当前配置：elucid                              │
│ Provider：custom                              │
│ Model：gpt-5.5                                │
│ Auth：auth_json                               │
│ [立即同步] [打开配置目录] [查看诊断详情]       │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│ 健康诊断                                      │
│ API Key       正常                            │
│ Base URL      正常                            │
│ Config File   正常                            │
│ Auth File     正常                            │
│ Plugin Path   正常                            │
└──────────────────────────────────────────────┘

配置列表
┌──────────────────────────────────────────────┐
│ elucid                         ● 使用中       │
│ API Key：sk-5fb••••••3b5b                    │
│ Base URL：https://hk.getelucid.com             │
│ Model：gpt-5.5                                │
│ [编辑] [复制] [备份]                 [删除]    │
└──────────────────────────────────────────────┘
```

### 修改重点

- “Codex 状态”卡片和“配置健康”卡片应整合为更清晰的状态模块；
- 诊断路径默认折叠；
- 点击“查看诊断详情”后展示完整路径和日志；
- 配置列表卡片统一视觉规范；
- 当前使用配置要有强识别；
- 删除操作必须二次确认；
- API Key 默认脱敏。

---

## 6.3 Claude Code 页面

### 页面目标

管理 Claude Code 环境变量、Cursor 配置、Claude 设置和同步状态。

### 当前问题

- “系统环境变量”“Cursor 设置”“Claude 设置”三个卡片并列，但其中 Cursor 失败状态被大面积红色背景强调，视觉过重；
- 用户不知道失败原因和修复路径；
- “当前配置”与三个配置状态之间关系不够清楚。

### 推荐布局

```text
Claude Code
管理 Claude Code API Key、Base URL 和编辑器环境变量。

[立即同步] [刷新诊断] [添加 Claude 配置]

当前配置
┌──────────────────────────────────────────────┐
│ 闪速ai                         ● 使用中       │
│ API Key：sk-a48••••••1226                    │
│ Base URL：https://opusrelay.com               │
└──────────────────────────────────────────────┘

环境状态
┌──────────────┬──────────────┬──────────────┐
│ 系统环境变量  │ Cursor 配置   │ Claude 设置   │
│ ● 已同步      │ ● 读取失败    │ ● 已同步       │
│ [查看]        │ [修复]        │ [查看]         │
└──────────────┴──────────────┴──────────────┘
```

### 修改重点

- 错误状态不要大面积红色铺底；
- 错误卡片显示简短原因 + 修复按钮；
- 详细错误放入展开面板；
- 环境变量和编辑器配置状态应该作为检测项，不是孤立卡片；
- “当前配置”应在页面更靠前的位置。

---

## 6.4 Configurations 配置管理页

### 页面目标

统一管理所有 Claude / Codex 配置。

### 推荐布局

```text
Configurations
集中管理 Claude Code 与 Codex CLI 配置。

[导入当前配置] [添加配置]

筛选：全部 / Claude / Codex / 使用中 / 异常
搜索配置名称 / Base URL / Provider

配置列表：
- 类型
- 名称
- 当前状态
- API Key
- Base URL
- Model
- 更新时间
- 操作
```

### 配置卡片 / 表格建议

窗口较宽时使用表格，窗口较窄时使用卡片。

表格列：

```text
类型 | 名称 | 状态 | API Key | Base URL | Model | 更新时间 | 操作
```

操作：

```text
切换使用 / 编辑 / 复制 / 导出 / 删除
```

### 修改重点

- 不要让 Claude 与 Codex 配置散落在不同页面无法统一管理；
- 添加搜索和筛选；
- 支持复制配置；
- 支持导出配置；
- 删除操作需要二次确认。

---

## 6.5 Plugin Market 插件市场

### 当前问题

当前插件市场在 Toolbox 弹窗内，使用较大的卡片区域展示，信息密度偏低，插件卡片露出不完整。

### 推荐布局

采用类似 VS Code 插件市场结构。

```text
Plugin Market
安装、启用和修复 Codex 插件。

┌─────────────────┬────────────────────────────────┐
│ 插件列表         │ 插件详情                         │
│ 搜索插件         │ Browser                         │
│ 分类筛选         │ 状态：已启用                      │
│ Browser          │ 描述                             │
│ Chrome           │ 版本 / 来源 / 依赖                │
│ ...              │ [启用] [禁用] [修复]              │
└─────────────────┴────────────────────────────────┘
```

### 修改重点

- 增加搜索框；
- 增加分类筛选：全部 / 已安装 / 未安装 / 已启用 / 需要修复；
- 左侧为插件列表，右侧为插件详情；
- 插件状态使用 Badge；
- 操作按钮固定在详情区；
- 插件源选择放在顶部，不要占据过大高度。

---

## 6.6 Session Sync 会话同步

### 当前问题

当前会话同步功能很强，但视觉像普通后台列表。搜索、全选、复制 ID、回收站、移动到回收站等按钮挤在一起。

### 推荐布局

```text
Session Sync
同步和管理本地 Codex 会话。

┌──────────────────────────────────────────────┐
│ 同步状态                                      │
│ 已同步 133 个会话                             │
│ 最近同步：刚刚                                │
│ 回收站：0 个                                  │
│ [立即同步] [打开目录] [导出备份]               │
└──────────────────────────────────────────────┘

搜索会话标题 / 项目 / 会话 ID
筛选：全部 / 已同步 / 回收站

会话列表
┌──────────────────────────────────────────────┐
│ variable-switching                           │
│ 项目：H:\variable-switching                  │
│ 更新时间：14 天前                             │
│ [复制 ID] [打开] [移入回收站]                 │
└──────────────────────────────────────────────┘
```

### 修改重点

- 顶部增加同步状态卡；
- 搜索与批量操作分区；
- 会话列表增加更清楚的字段；
- 回收站作为筛选或单独二级页面；
- 复制 ID 操作给 Toast 反馈；
- 移入回收站必须可撤销。

---

## 6.7 Mobile Control 手机控制

### 当前问题

当前手机控制页面左侧是选择应用和控制对话，右侧是微信绑定状态。整体逻辑可用，但“手机控制的工作流程”不够直观。

### 推荐布局

```text
Mobile Control
通过微信 / 飞书 / QQ 将手机消息转发到 Codex 会话。

┌──────────────────────────┬──────────────────────────┐
│ 控制设置                  │ 连接状态                  │
│ 绑定应用：微信             │ ● 微信已绑定              │
│ 控制会话：xxx              │ ● 平台监听中              │
│ [绑定微信] [清除绑定]      │ ● 等待手机消息            │
│                           │                           │
│ 高级控制通道：未连接        │ 最近消息：暂无             │
└──────────────────────────┴──────────────────────────┘

底部操作：
[停止监听] [开启平台连接]
```

### 修改重点

- 明确展示流程：绑定应用 → 选择会话 → 开启监听 → 等待消息；
- 右侧增加状态时间线；
- “绑定微信”和“清除绑定”不要同时作为同级主按钮；
- “开启平台连接”作为主按钮；
- “停止”作为次级或危险操作；
- 如果微信已绑定，应显示绑定状态和重新绑定入口。

---

## 6.8 Settings 设置页

### 页面目标

承载所有低频配置和辅助入口。

推荐分组：

```text
General 通用
- 语言
- 主题
- 启动时检查状态
- 开机自启动

Paths 路径
- Codex 配置目录
- Claude 配置目录
- 插件目录
- 会话目录

Appearance 外观
- 浅色 / 深色 / 跟随系统
- 紧凑模式

Help 帮助
- 使用说明
- 检查更新
- 下载网站
- GitHub 仓库
- 关于 VarSwitch
```

### 修改重点

- 从顶部栏移除语言和主题，放入 Settings；
- GitHub、下载网站、检查更新进入 Help 分组；
- 路径类配置集中管理；
- 危险设置如清空数据需要二次确认。

---

## 7. 交互规范

### 7.1 同步操作

点击“立即同步”后：

1. 按钮进入 loading 状态；
2. 禁止重复点击；
3. 页面状态局部刷新；
4. 成功显示 Toast；
5. 失败显示错误摘要 + 查看详情。

推荐 Toast：

```text
同步成功
Codex CLI 已切换到 elucid 配置。
```

失败示例：

```text
同步失败
无法写入 ~/.codex/config.toml，请检查文件权限。
[查看详情]
```

### 7.2 删除操作

删除配置前必须二次确认。

Dialog：

```text
删除配置 “elucid”？

删除后将无法从 VarSwitch 中恢复，但不会自动删除你的远程 API 服务。
如果该配置当前正在使用，删除前需要先切换到其他配置。

[取消] [删除配置]
```

### 7.3 切换配置

切换配置后：

- 显示 loading；
- 检查写入是否成功；
- 刷新当前状态；
- 显示 Toast；
- 如果失败，恢复旧状态。

### 7.4 复制敏感信息

点击复制 API Key 或 Base URL：

- API Key 不直接显示完整内容；
- 复制成功显示 Toast；
- 支持“显示 10 秒”功能。

### 7.5 错误详情

错误不要直接把完整技术路径暴露在首页。

首页只显示：

```text
Cursor 配置读取失败
[修复] [查看详情]
```

详情中再展示：

```text
错误原因：文件不存在
路径：C:\Users\...\cursor\config.json
建议：请确认 Cursor 是否已安装，或手动选择配置路径。
```

---

## 8. 敏感信息与安全展示规范

### 8.1 API Key 展示

默认展示格式：

```text
sk-5fb••••••3b5b
```

禁止默认展示完整 Key。

提供操作：

```text
复制
显示
隐藏
```

显示完整 Key 时：

- 需要用户点击；
- 10 秒后自动隐藏；
- 可以显示安全提示。

### 8.2 Base URL 展示

Base URL 可以显示，但长 URL 需要省略：

```text
https://hk.getelucid.com
https://code.strova.top:8443/v1
```

过长时使用 tooltip 展示完整内容。

### 8.3 本地路径展示

路径属于技术细节，默认折叠。

展示方式：

```text
配置文件：正常
认证文件：正常
插件目录：正常
[查看路径详情]
```

展开后显示完整路径。

---

## 9. Codex 可执行重构任务清单

以下内容可以直接交给 Codex 作为实施任务。

### 9.1 第一阶段：基础布局重构

```text
任务：将 VarSwitch 主界面从顶部 Tab + 页面堆叠结构重构为桌面控制台结构。

要求：
1. 新增左侧 Sidebar 导航。
2. Sidebar 包含 Overview、Claude Code、Codex CLI、Configurations、Plugin Market、Session Sync、Mobile Control、Settings。
3. 顶部 Top Bar 只保留 Logo、产品名、当前配置状态、导入配置、添加配置、设置入口。
4. 移除首页大 Hero 欢迎区，将其改为 Overview 状态总览。
5. 保持现有功能逻辑不丢失，只调整信息架构和布局。
6. 所有导航切换不应刷新整个应用状态。
7. 当前选中的导航项需要有明显 active 状态。
```

### 9.2 第二阶段：组件规范统一

```text
任务：统一 VarSwitch 的 Button、Card、Badge、Dialog、Input、Toast 组件风格。

要求：
1. 建立统一 Button 组件：primary、secondary、ghost、danger、icon。
2. 建立统一 Card 组件，包含 header、body、footer。
3. 建立统一 StatusBadge 组件，支持 healthy、error、warning、info、disabled、active。
4. 建立统一 Dialog 组件，替代所有原生 alert、confirm、prompt。
5. 建立统一 Toast 反馈机制。
6. 所有 icon-only 按钮必须有 Tooltip。
7. 所有危险操作必须使用 danger 样式并二次确认。
```

### 9.3 第三阶段：Overview 页面实现

```text
任务：实现新的 Overview Dashboard。

要求：
1. 展示当前主配置名称。
2. 展示 Claude Code 状态。
3. 展示 Codex CLI 状态。
4. 展示系统环境变量同步状态。
5. 展示插件状态、会话同步状态、手机控制状态。
6. 提供快捷操作：立即同步、添加配置、导入配置、查看诊断、打开插件市场。
7. 添加最近事件列表。
8. 首页不展示完整 API Key 和完整本地路径。
9. 异常状态提供“查看详情”入口。
```

### 9.4 第四阶段：Codex CLI 页面重构

```text
任务：重构 Codex CLI 页面。

要求：
1. 页面顶部展示当前 Codex CLI 状态卡。
2. 状态卡包含当前配置、Provider、Model、Auth、健康状态。
3. 操作按钮包含立即同步、刷新诊断、备份 Codex 文件、添加 Codex 配置。
4. 健康诊断项以列表展示，不直接大面积展示路径。
5. 路径信息默认折叠，点击查看详情后展开。
6. 配置列表卡片统一样式。
7. 当前使用配置显示绿色状态点和“使用中”标签。
8. API Key 默认脱敏。
9. 删除配置需要二次确认。
```

### 9.5 第五阶段：Claude Code 页面重构

```text
任务：重构 Claude Code 页面。

要求：
1. 页面顶部展示当前 Claude 配置。
2. 展示系统环境变量、Cursor 配置、Claude 设置三个检测项。
3. 错误状态不要使用大面积红色背景，只使用红色状态标签和修复按钮。
4. Cursor 读取失败时展示“修复”和“查看详情”。
5. 支持立即同步和刷新诊断。
6. API Key 默认脱敏。
```

### 9.6 第六阶段：添加配置向导

```text
任务：将添加 Codex 配置弹窗改为分步骤向导。

要求：
1. Step 1：选择预设。
2. Step 2：选择写入方式。
3. Step 3：填写连接信息。
4. Step 4：测试连接。
5. Step 5：保存或保存并启用。
6. 弹窗不应出现内部长滚动。
7. 每一步都有清楚说明。
8. 表单字段必须校验。
9. Base URL 支持测试。
10. API Key 默认密码显示。
```

### 9.7 第七阶段：替换原生弹窗

```text
任务：移除所有浏览器原生 alert、confirm、prompt。

要求：
1. 搜索代码中所有 alert()、confirm()、prompt()。
2. 用统一 Dialog 或 Toast 替换。
3. 导入当前配置名称输入改为自定义 Dialog。
4. 删除确认改为危险确认 Dialog。
5. 操作成功和失败改为 Toast。
6. 不允许再出现 tauri.localhost 原生弹窗。
```

### 9.8 第八阶段：Toolbox 拆分

```text
任务：将 Codex Toolbox 中的插件市场、会话同步、手机控制拆分为独立页面或二级导航。

推荐做法：
1. Plugin Market、Session Sync、Mobile Control 提升为一级导航。
2. 如果保留 Toolbox 弹窗，则内部改为左侧二级导航。
3. 不再使用顶部横向 Tab 承载这三个复杂功能。
```

### 9.9 第九阶段：插件市场重构

```text
任务：重构 Plugin Market 页面。

要求：
1. 使用左侧插件列表 + 右侧插件详情布局。
2. 增加搜索框。
3. 增加筛选：全部、已安装、未安装、已启用、需要修复。
4. 插件状态使用 Badge。
5. 插件操作包含安装、启用、禁用、修复。
6. 插件源选择放在顶部区域。
```

### 9.10 第十阶段：会话同步重构

```text
任务：重构 Session Sync 页面。

要求：
1. 顶部展示同步状态卡。
2. 展示已同步会话数量、最近同步时间、回收站数量。
3. 提供立即同步、打开目录、导出备份操作。
4. 搜索框支持标题、项目、消息或会话 ID。
5. 会话列表项展示标题、项目路径、更新时间、操作。
6. 支持复制 ID、打开、移入回收站。
7. 移入回收站提供撤销 Toast。
```

### 9.11 第十一阶段：手机控制重构

```text
任务：重构 Mobile Control 页面。

要求：
1. 页面分为控制设置和连接状态两栏。
2. 控制设置包含绑定应用和控制会话。
3. 连接状态展示：平台已绑定、监听中、等待手机消息。
4. 增加状态时间线。
5. 开启平台连接作为主按钮。
6. 停止监听作为次级或危险按钮。
7. 清除绑定需要二次确认。
```

---

## 10. Codex 总提示词

下面这段可以直接复制给 Codex 使用。

```text
请对当前 VarSwitch 项目进行一次专业级 UI/UX 重构，目标是将现有“功能堆叠型工具页”升级为“AI 开发环境控制台”。

产品定位：
VarSwitch 是一个用于管理 Claude Code、Codex CLI、插件市场、会话同步、手机控制和多套 API 配置的桌面端控制台应用。

核心目标：
1. 用户打开软件后能第一时间看到当前环境是否健康。
2. 用户能清楚知道当前使用的是哪个配置。
3. 用户能快速完成添加配置、导入配置、切换配置、立即同步、诊断修复等操作。
4. 所有功能模块的信息架构清晰，不再堆叠在同一个首页。
5. UI 风格专业、干净、克制，参考 Linear、Raycast、Vercel Dashboard、ChatGPT Settings。

必须完成的结构改动：
1. 新增左侧 Sidebar 主导航，包含：Overview、Claude Code、Codex CLI、Configurations、Plugin Market、Session Sync、Mobile Control、Settings。
2. 顶部 Top Bar 只保留 Logo、产品名、当前配置状态、导入配置、添加配置、设置入口。
3. 将原首页大 Hero 区改为 Overview Dashboard，展示当前配置、Claude 状态、Codex 状态、环境变量状态、插件状态、会话同步状态、手机控制状态、快捷操作和最近事件。
4. 将 Claude Code 和 Codex CLI 从横向 Tab 改为独立页面。
5. 将 Codex Toolbox 中的插件市场、会话同步、手机控制拆分为独立页面，或至少改为左侧二级导航结构。
6. 添加配置弹窗改为分步骤向导：选择预设、选择写入方式、填写连接信息、测试连接、保存并启用。
7. 禁止使用浏览器原生 alert、confirm、prompt，全部替换为统一 Dialog 和 Toast。
8. 所有 API Key 默认脱敏显示，支持复制和临时显示。
9. 本地路径和诊断详情默认折叠，点击查看详情后展开。
10. 删除配置、清除绑定、移入回收站等危险操作必须二次确认或支持撤销。

视觉规范：
- 背景色使用 #F7F8FA。
- 卡片背景使用 #FFFFFF。
- 边框使用 #E5E7EB。
- 主文字使用 #111827。
- 次级文字使用 #6B7280。
- 主色使用 #2563EB。
- 成功色使用 #16A34A。
- 警告色使用 #D97706。
- 错误色使用 #DC2626。
- 页面 Padding 24px。
- 卡片圆角 14px 或 16px。
- 按钮高度 36px 或 40px。
- 输入框高度 40px。
- 减少大面积渐变和毛玻璃，渐变只用于 Overview 状态总览或空状态。

组件要求：
1. 建立统一 Button 组件，支持 primary、secondary、ghost、danger、icon。
2. 建立统一 Card 组件，支持 header、body、footer。
3. 建立统一 StatusBadge 组件，支持 healthy、error、warning、info、disabled、active。
4. 建立统一 Dialog 组件，替代所有原生弹窗。
5. 建立统一 Toast 反馈机制。
6. 所有 icon-only 按钮必须有 Tooltip。

页面要求：
1. Overview：展示全局健康状态、当前配置、快捷操作、最近事件。
2. Claude Code：展示当前配置、系统环境变量、Cursor 配置、Claude 设置、同步和诊断操作。
3. Codex CLI：展示当前 Codex 配置、健康诊断、配置列表、同步和备份操作。
4. Configurations：统一管理所有 Claude / Codex 配置，支持搜索、筛选、导入、添加、编辑、复制、导出、删除。
5. Plugin Market：使用左侧插件列表 + 右侧插件详情布局，支持搜索、筛选、安装、启用、禁用、修复。
6. Session Sync：展示同步状态、会话数量、最近同步、搜索、会话列表、复制 ID、移入回收站。
7. Mobile Control：展示绑定应用、控制会话、平台连接状态、监听状态、消息状态。
8. Settings：承载语言、主题、路径、检查更新、GitHub、下载网站、使用说明、关于等低频入口。

实现要求：
- 尽量复用现有业务逻辑，不破坏原有功能。
- 先重构布局和组件，再迁移具体页面。
- 每个页面保持状态清晰、操作明确、异常可修复。
- 改动完成后检查是否仍存在 alert、confirm、prompt。
- 确保浅色主题可用，深色主题如果已有则同步适配。
```

---

## 11. 验收标准

改版完成后，使用以下标准验收。

### 11.1 信息架构验收

- 是否存在清晰左侧导航；
- 是否能一眼找到 Claude Code、Codex CLI、插件市场、会话同步、手机控制；
- 首页是否不再堆叠所有功能；
- 帮助、更新、GitHub、下载是否已移到设置或帮助区域。

### 11.2 视觉一致性验收

- 按钮样式是否统一；
- 卡片样式是否统一；
- Badge 状态颜色是否统一；
- 弹窗样式是否统一；
- 页面间距是否统一；
- 是否减少了不必要的大面积渐变。

### 11.3 交互验收

- 添加配置是否为分步骤向导；
- 删除是否有确认；
- 复制是否有 Toast；
- 同步是否有 loading；
- 错误是否有查看详情；
- 原生 alert / confirm / prompt 是否全部移除。

### 11.4 安全展示验收

- API Key 是否默认脱敏；
- 完整 Key 是否不会默认暴露；
- 路径是否默认折叠；
- 敏感操作是否二次确认。

### 11.5 功能完整性验收

- Claude Code 配置仍可读取、同步；
- Codex CLI 配置仍可读取、同步；
- 添加配置仍可保存；
- 导入配置仍可使用；
- 插件市场仍可安装和修复；
- 会话同步仍可搜索、复制、移动；
- 手机控制仍可绑定、监听、停止。

---

## 12. 改版优先级建议

### P0 必须优先完成

1. 替换原生 alert / confirm / prompt；
2. API Key 默认脱敏；
3. 添加左侧导航；
4. 首页改为 Overview Dashboard；
5. 添加配置改为分步骤向导。

### P1 第二优先级

1. Codex CLI 页面重构；
2. Claude Code 页面重构；
3. 配置管理页新增；
4. 统一 Button / Card / Badge / Dialog / Toast。

### P2 第三优先级

1. 插件市场改为列表 + 详情；
2. 会话同步页面优化；
3. 手机控制状态时间线；
4. Settings 页面整理。

### P3 后续增强

1. 全局搜索 / Command Palette；
2. 操作日志；
3. 配置版本历史；
4. 一键导出诊断报告；
5. 新手引导；
6. 紧凑模式；
7. 深色主题精细适配。

---

## 13. 最终设计目标

VarSwitch 改版后的体验应该是：

```text
状态清楚
路径清楚
操作清楚
错误清楚
下一步清楚
```

而不是：

```text
功能都在，但用户需要自己找
```

最终产品气质应该接近：

- Linear 的清晰层级；
- Raycast 的工具效率；
- Vercel Dashboard 的专业控制台风格；
- ChatGPT Settings 的简洁表单体验；
- VS Code Extensions 的插件管理逻辑。

这次重构的核心不是“更炫”，而是让 VarSwitch 变成一个真正专业、可扩展、可维护、对用户友好的 AI 开发环境控制台。
